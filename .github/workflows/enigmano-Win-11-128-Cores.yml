name: "⚡ Custom RDP Session (4-Core, 16GB RAM) - Corrected"

on:
  workflow_dispatch:

jobs:
  build:
    name: "🚀 Start Windows RDP Session"
    runs-on: windows-latest

    env:
      NGROK_AUTH_TOKEN: ${{ secrets.NGROK_SHAHZAIB }}

    steps:
      - name: "🚀 Setup RDP, Firewall, and Start Ngrok"
        id: setup
        shell: pwsh
        run: |
          # Create user and enable RDP
          $password = "your-strong-password-here" # Change this to a secure password
          $user = "runneradmin"
          iex "net user /add $user $password"
          iex "net localgroup administrators $user /add"
          iex "reg add 'HKLM\System\CurrentControlSet\Control\Terminal Server' /v fDenyTSConnections /t REG_DWORD /d 0 /f"
          iex "reg add 'HKLM\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' /v UserAuthentication /t REG_DWORD /d 1 /f"
          Write-Host "✅ User account created and RDP enabled."

          # Download and set up the latest Ngrok version
          Invoke-WebRequest -Uri "https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-windows-amd64.zip" -OutFile "ngrok.zip"
          Expand-Archive ngrok.zip -DestinationPath "ngrok"
          
          # Add a firewall rule for Ngrok
          New-NetFirewallRule -DisplayName "Allow ngrok" -Direction Inbound -Program "D:\a\${{ github.repository }}\${{ github.repository }}\ngrok\ngrok.exe" -Action Allow
          
          cd ngrok

          # Authenticate and start the tunnel in a specific region
          ./ngrok.exe authtoken $env:NGROK_AUTH_TOKEN
          Start-Process -FilePath ".\ngrok.exe" -ArgumentList "tcp 3389 --log ../ngrok.log --region us"
          
          Write-Host "✅ Ngrok setup complete. Waiting 20 seconds for tunnel..."
          
          # Wait 20 seconds for Ngrok to initialize and write the log
          Start-Sleep -s 20
          
          # Read the log file to find the URL
          if (Test-Path -Path "../ngrok.log") {
              $logContent = Get-Content -Path "../ngrok.log"
              Write-Host "--- Ngrok Log Content ---"
              $logContent | ForEach-Object { Write-Host $_ }
              Write-Host "--- End of Ngrok Log ---"

              $rdpConnection = ($logContent | Select-String -Pattern "url=(tcp://.+)" | Select-Object -First 1).Matches.Groups[1].Value
              
              if ($rdpConnection) {
                  Write-Host " "
                  Write-Host "✅ RDP Connection Ready!"
                  Write-Host "   Connect to: $rdpConnection"
                  Write-Host "   Username: runneradmin"
                  Write-Host "   Password: your-strong-password-here"
                  Write-Host " "
              } else {
                  Write-Error "❌ CRITICAL: Could not find Ngrok URL in the log. The tunnel failed to start. Check the log for errors."
                  exit 1
              }
          } else {
              Write-Error "❌ CRITICAL: ngrok.log file was not created. Ngrok failed to launch."
              exit 1
          }

      - name: "⏳ Keep RDP Session Alive"
        shell: pwsh
        run: |
          Write-Host "✅ RDP session is active. This job will continue for up to 6 hours."
          Write-Host "   To end the session, cancel the GitHub Actions workflow."
          Start-Sleep -s 21000
          Write-Host "   To end the session, cancel the GitHub Actions workflow."
          Start-Sleep -s 21000 # Keep alive for just under 6 hours
